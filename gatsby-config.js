module.exports = {
  siteMetadata: {
    title: "leandre_s_blog",
  },
  plugins: [
    // scroll parallax:
    `gatsby-plugin-sass`,
    // images:
    `gatsby-plugin-image`,
    `gatsby-plugin-sharp`,
    `gatsby-transformer-sharp`],
};
